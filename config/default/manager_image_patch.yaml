apiVersion: apps/v1
kind: Deployment
metadata:
  name: kserve-controller-manager
  namespace: kserve
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: default-route-openshift-image-registry.apps.pierdipi.osp.rh-ods.com/opendatahub/kserve-controller:27429f09b
        name: manager
